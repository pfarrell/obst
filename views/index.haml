%div.row
  %div.onecol
    &nbsp;
  %div.tencol.last
    %div#container
      %span#stratagem
%div.row
  %div.fourcol
    &nbsp;
  %div.twocol
    %a#reload
      &rArr;
  %div.twocol
    %span#about
      what is this?
  %div.fourcol.last
    &nbsp;
:javascript
  $('#reload').bind('click', function() {
    var stratagem = $('#stratagem')
    $.post(
      "#{url_for('/')}",
      '',
      function(INFO) {
        stratagem.data('fsr', INFO);
        set_stratagem(stratagem);
      },
      'json'
    );
  });
  $('#stratagem').each(function(i) {
    var stratagem = $('#stratagem');
    $.post(
      "#{url_for('/')}",
      '',
      function(INFO) {
        $(stratagem).data('fsr', INFO);
        set_stratagem(stratagem);
      },
      'json'
    );
  });
  function set_stratagem(stratagem){
    $('#stratagem').text(stratagem.data('fsr').gem);
  }
 
  
